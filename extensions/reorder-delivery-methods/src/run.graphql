query RunInput {
  deliveryCustomization {
    metafield(
      namespace: "$app:delivery-customization"
      key: "order"
    ) {
      value
    }
  }
  cart {
    deliveryGroups {
      deliveryOptions {
        handle
        title
      }
    }
  }
}
